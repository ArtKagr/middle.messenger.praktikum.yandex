export default `
        <div class="{{ className }}">
            <span class="{{ chatsTitle }}" onClick="{{ handleClick }}">{{ text }}</span>
            <span>{{ user.info.firstName }}</span>
    `
